/* Store that contains the global down/up speeds */

let config = require('./../../../../config.json')

const state = config.globalSpeedCaps

const mutations = {}

const actions = {}

export default {
  state,
  mutations,
  actions
}
